# Feature Index

> Central tracking for all features. Updated by skills automatically.
> Migration context: features below are being ported from `/OLD APP` (Lovable / TanStack Start) into Next.js. See `docs/MIGRATION.md`.

## Status Legend
- **Roadmap** - `/init` done, feature identified in feature map, no spec file yet
- **Planned** - `/write-spec` done, full spec written, architecture not yet designed
- **Architected** - `/architecture` done, tech design approved, ready to build
- **In Progress** - `/frontend` or `/backend` active or completed, not yet in QA
- **In Review** - `/qa` active, testing in progress
- **Approved** - `/qa` passed, no critical/high bugs, ready to deploy
- **Deployed** - `/deploy` done, live in production

## Features

> **Status-Audit 2026-06-12 (PROJ-11):** Stati an die Realität angeglichen — die V1-Kernmodule sind live auf objekt-pilot.vercel.app. **V1-Scope:** aktiv sind Objekte, Kunden, Reservierungen, Team, Profil, Einstellungen/Org-Branding. **V2-zurückgestellt** (im Front-End ausgegraut/gegated, Code bleibt): Provisionen, Finanzierungen, KI. Supabase-Advisors: **keine ERROR-Level-Befunde** (RLS intakt); offene Tuning-/Config-Punkte siehe `docs/USER-TODO.md`. Formale Per-Feature-QA (E2E) steht noch aus.

> **QA-Pass 2026-06-12:** Unit-Tests für finanzkritische Kernlogik angelegt (`npm test` → **107/107 grün**, 10 Dateien), Build (Typecheck) grün, Security-Audit der Server-Action-Autorisierung durchgeführt. Voller Bericht: `docs/QA-RESULTS-2026-06-12.md`.
> **Fixes 2026-06-12:** **BUG-001 (High)** — Objekt-CRUD umging Mandanten-Isolation (Admin-Client-Writes per id ohne Org-Check) → neuer Guard `assertOrgAccess()` in `src/lib/actions/_org.ts`, in allen Objekt-CRUD-Writes erzwungen (admin/support: Cross-Org per Konvention). **BUG-002 (Low)** — `createProjekt` lehnt jetzt null-Org ab. Regressionstest `src/lib/actions/_org.test.ts`. **Offen:** E2E (Playwright, braucht Staging+Seed), Advisor-WARN-Tuning (`docs/USER-TODO.md`). ~~`npm run lint` defekt~~ **erledigt** — Script ist `eslint .` (Flat-Config), läuft mit 0 Errors (nur Warnings).

> **Performance + UI-Entrümpelung (2026-06-17, PROJ-3/PROJ-21, cross-cutting):** Nutzer-Feedback „Ladezeiten an vielen Stellen zu lang" + „Einheiten-Detail unübersichtlich". **(1) RLS-Init-Plan-Fix (systemisch):** Migration `20260617000000_rls_initplan_wrap_auth_functions` umschließt `auth.uid()`/`current_org_id()` in ALLEN Policies mit `(select …)` → Postgres wertet sie 1× pro Statement (InitPlan) statt 1× pro Zeile aus. Reine Auswertungs-Timing-Änderung, Bool-Logik byte-identisch → RLS-Semantik unverändert (verifiziert: sichtbare Zeilen pro Rolle gleich — admin 2108, vp_l3 18). Behebt ~90× Advisor `auth_rls_initplan`. Full-Table-Policy-Scan auf `einheiten` 104→47 ms; Admin-2108-Zeilen-Query ~200 ms DB-Zeit. Betrifft jede Tabelle → der eigentliche „überall langsam"-Hebel. **(2) Instant-Skeletons:** neue `loading.tsx` für `/objekte` + `/objekte/projekt/[id]` (vorher blockierte die Navigation ohne Feedback). **(3) Entrümpelung Einheiten-Detail:** `CompletenessCard` kompakt (Pflichtfeld-% inline, „Fehlende Daten" als ausklappbares `<details>` statt Dauer-Liste, Zusatz-Signale als kleine Chips); `ProjektTabs`-Master-Detail blendet Verkaufsstatus-Breakdown bei ≤1 Einheit aus und rendert das Detail bei genau 1 Einheit ohne 320px-Liste (Splitscreen nur ab >1). tsc + 184 Tests + Build grün. **Deployed 2026-06-17** (Commit `3f57e79`, Push auf `main` → Vercel; Smoke-Test `/login` 200, `/objekte` 307). DB-Migration war bereits live auf Prod-DB appliziert. **Offen:** visuelle Prod-Verifikation (eingeloggter VP), E2E PROJ-16/21; optional Skeletons für weitere Routen (Kunden/Dashboard).

> **Projektansicht v2 „Klar" (B1) Redesign (2026-06-18, PROJ-3, UI):** `ProjektDetailView.tsx` komplett auf das High-Fidelity-Handoff „Variante B1 Klar" umgebaut (warme Paper-App-Surface #f5f2ec radius16 mit Editorial-Schatten, Spectral-Serif für Titel/Preise/Großmetriken + Manrope für UI via `next/font/google` in `layout.tsx` → CSS-Vars `--font-spectral`/`--font-manrope`). **Single-Scroll-Layout:** (1) Galerie-Hero (1 groß flex1.7 + 2 gestapelt, schwebende 42px-Rundbuttons Zurück/Merken/Teilen/Mehr, Dokument-Overlay-Chip, „+N Fotos"-Badge), (2) Zwei-Spalten-Body (links flex1 / rechts 340px sticky top:5): Titelblock mit Gold-Eyebrow + Pin-Box + „Auf Karte zeigen", 4 Großmetriken (Kaufpreis in Champagne-Pill #f0e3c8, Wohnfläche, Einheiten·N frei, Bruttorendite), Chip-Reihe (Finanzieren-Outline-Button, Provisionsfrei, Bestand), Einheiten-Auswahl-Dropdown mit Status-Dots, Verkaufsstatus-Segmentbar, Vermögens-Chart, Objektdaten-Tabelle; rechts Action-Card (Angebotspreis Spectral36 navy, Anfragen-CTA, Preis vorschlagen, Exposé/Präsentation/Bearbeiten, „Ihre Investition"-Box, Berater). **Funktionserhalt:** Back-Link, Deep-Link `?einheit=`, Einheiten-Auswahl treibt Preis/Chart/Investition/Objektdaten (lazy `getEinheitDetailAction`); **Chart & Investitions-Zahlen wiederverwendet** über `useEinheitKalkulation` + `DiagrammePanel` (keine SVG-Neubaute); `ProjektTabs` (Lage-Karte/Bankdaten/Finanzierer-Pool/volles Einheiten-Detail) bleibt unter dezentem Divider erhalten — nichts verloren. tsc + Build + **219 Tests** + Lint (0 Errors) grün. **Offen:** Deploy auf Vercel, visuelle Prod-Verifikation (eingeloggter VP), „Anfragen"/„Preis vorschlagen"/Exposé-Verdrahtung an echte Flows (aktuell Präsentationsroute/Platzhalter).

> **Objekte-Ladezeit-Fix (Root-Cause) + Investagon-Detail + Domain (2026-06-17, PROJ-3, cross-cutting):** Nutzer meldete `TimeoutError: operation aborted` beim Laden von Objekten. **Diagnose (an Prod-DB gemessen):** Die Liste lud bei *jedem* Aufruf alle ~2108 Einheiten der Org ungepaged (`listObjekte`); in `pg_stat_statements` Ø **2095 ms / max 4822 ms**, warm im EXPLAIN nur ~200 ms → die Differenz ist **Contention** (Postgres-Logs voll von `canceling statement due to statement timeout`, DB einmal abgestürzt/recovered; `statement_timeout` 8 s gepoolt). Ein Covering-Index brachte **nichts** (getestet+verworfen): die RLS-Regel `org = $org OR ($org IS NULL)` blockiert jeden Index-Range-Scan; zudem sind alle 2108 Einheiten in *einer* Org → kein Prädikat drückt unter 2108. **Durable Fix (umgesetzt):** Migration `20260617020000_projekt_einheiten_aggregat` denormalisiert die Listen-Aggregate (count, Preis-/Flächen-/Zimmer-/AfA-Spannen, €/m², Miete/m², status_counts) per Trigger auf `projekte.einheiten_aggregat` (jsonb). Neue Datenfn `listProjekteUebersicht()` liest nur noch ~224 projekte-Zeilen (~300 ms warm), **kein Einheiten-Scan auf dem heißen Pfad mehr**. Liste auf **Projekt-first** umgebaut (`ObjekteListView` rendert `ProjektCard` aus aggregat, neuer schlanker Projekt-Filter statt `applyObjekteFilters`; Einheiten-Tabelle entfällt — Einheiten leben auf der Projekt-Detailseite). `ObjektCard.tsx`/`ObjekteFilterSidebar.tsx` jetzt verwaist (belassen). **Investagon-Stil Detail:** neue `EinheitInvestmentView` + `EinheitKalkulationPanel` (Hook `useEinheitKalkulation`): beim Öffnen einer Einheit sofort Vermögens-Chart (Tabs Diagramme|Kalkulation|Annahmen, Diagramme default) + sticky Investitions-Sidebar; ein gemeinsamer Calc-State → Slider aktualisieren Chart+Sidebar live. `calculate()`-Engine unverändert wiederverwendet. **Domain:** App ist domain-agnostisch (`resolveSiteUrl()` nutzt Request-Origin); Umstellung auf `portal.erfolg-mit-immobilien.com` ist reine Config — externe Schritte in `docs/USER-TODO.md`. tsc + Build + Lint (0 Errors) + **206 Tests** grün. **Migration ist bereits live auf Prod-DB** (rückwärtskompatibel). **Offen:** Code-Deploy auf Vercel (sonst läuft Prod weiter auf der langsamen `listObjekte`), visuelle Verifikation (eingeloggter VP), E2E.

> **V2-Features reaktiviert (2026-06-18, PROJ-6/PROJ-9):** Nutzerwunsch „Finanzierer & Provisionen aktivieren". **Finanzierungen (PROJ-6)** und **Provisionen (PROJ-9)** waren in V1 nur soft-gated (Nav-`hidden`-Flag + `FeatureComingSoon`-Platzhalter auf den Routen); der echte View- + Daten-Layer war die ganze Zeit unangetastet im Code. Reaktivierung = reines Umlegen der Schalter: `hidden`-Flags aus `src/lib/navigation.ts` entfernt (beide tauchen wieder in Sidebar/CommandPalette/MobileTabbar auf, gescopt auf ihre Rollen — Finanzierungen auch für `finanzierer`); die drei Routen (`/finanzierungen`, `/finanzierungen/[caseId]`, `/provisionen`) auf ihre Original-Verdrahtung zurückgesetzt (`listCasesForFinanzierer`/`getCase`/`listCaseKommentare` → `FinanzierungenListView`/`CaseDetailView`; `listProvisionen`/`provisionenSummary` → `ProvisionenListView`). **Kundenportal (PROJ-7)** war bereits live und nicht gegated — keine Änderung nötig. Sicherheit unverändert: RLS/RPC-Schichtung der Finanzierungs-Cases war laut QA-Audit (2026-06-15) schon korrekt (Mandanten-Isolation + Granular-Policies), der Soft-Gate war nie die einzige Schutzschicht. tsc + **206 Tests** + Build grün (beide Routen jetzt dynamisch server-gerendert). **Offen:** Deploy auf Vercel/Prod (sonst weiter „Bald"-Platzhalter live), manuelle Verifikation als eingeloggter VP/Finanzierer, optional die Low-Hygiene-Findings BUG-F1 (anon-EXECUTE-REVOKE) aus dem QA-Audit.

> **Production-Readiness-Pass + KI gebaut (2026-06-18, cross-cutting):** Nutzerwunsch „App komplett production-ready/professionell" + „baue alle KI-Features". **(A) Security-Sweep:** alle 18 Server-Action-Dateien (80 Writes) per 4 parallelen Audit-Agents + Live-RLS-Verifikation auf die BUG-P1-Klasse geprüft (Admin-Client/RLS-Bypass-Writes ohne Org-/Ownership-Guard). Befund: **nur `provisionen.updateProvisionStatus`** hatte einen echten High (cross-org/cross-tree Status-Write) → **gefixt** (org+tree-Guard, siehe PROJ-9). Übrige Admin-Client-Writes sind korrekt mit `assertOrgAccess`/Ownership gegated. Live-DB bestätigt: `org_isolation` (RESTRICTIVE) + saubere VP/VL-Policies auf kunden/einheiten/reservierungen/okz vorhanden (Repo-Migrationen waren unvollständig, Prod nicht). **Zusätzlich gehärtet:** branding-extract **SSRF-Redirect-Bypass** (Medium) — Redirects manuell + jeden Hop neu validiert + Schema-Check; **Selbstauskunft-Immutabilität** (BUG-S1, Medium) — bereits `eingereicht` wird serverseitig abgelehnt (public + authed); **BUG-S2** noindex auf `/selbstauskunft/[token]`; org-Owner nicht mehr durch org-admin entfernbar + app-level Rollen-Precheck; `setZuweisungStatus` internes Rollen-Gate; `createReservierung` expliziter Kunden-Ownership-Check (schließt activateKundePortalCore-IDOR). **(B) PROJ-22 KI gebaut:** dependency-freier OpenAI-Client (`src/lib/ai/openai.ts`, fetch, liest `OPEN_API_KEY` mit Fallback `OPENAI_API_KEY`, Modell via `OPENAI_MODEL`, Default `gpt-4o-mini`); reine Prompt-/Parse-Logik (`lageeinschaetzung.ts`, 7 Tests); Actions `generateEinheitLage`/`saveEinheitKiFelder`/`getAiStatus` (interne Rollen + assertOrgAccess); „Per KI generieren"-Button im EinheitForm-Extras-Tab (füllt standort_highlights + tags als Vorschlag); KI-Lageeinschätzung fließt in die Präsentations-Lage-Slide. `kiPflichtAktiv` (Freigabe-Gate) bewusst NICHT scharfgeschaltet (würde Bestands-Einheiten blockieren). tsc + **213 Tests** + Build + Lint (0 Errors) grün. **Offen:** Deploy auf Vercel; Live-KI-Verifikation (Key nur in .env.local/Vercel); **Hinweis Env-Var:** Nutzer setzte `OPEN_API_KEY` statt Standard `OPENAI_API_KEY` — Client unterstützt beide. **Decision-needed (nicht auto-gefixt):** createInvite uncapped vp_l1-Provisionssatz, investagon_sync_log nicht org-scoped (falsches Inkrement-Fenster), Rate-Limit auf Public-Selbstauskunft, OpenAI-Key-at-rest plaintext.

> **E-Mail-Versand auf Resend-direkt umgestellt (2026-06-18, PROJ-1/PROJ-5/PROJ-7):** Nutzer hat `RESEND_API_KEY` in `.env.local`/Vercel gesetzt („Resend klappt jetzt"). Problem: Der bisherige Versand lief über **Supabase-Edge-Functions** (`send-invite/portal/reservation-email`), die das Secret als **Supabase-Secret** + Deployment bräuchten — der Next.js-Key half ihnen nicht. **Fix:** zentrale E-Mail-Schicht in Next.js (`src/lib/email/resend.ts` — `sendEmail()` via Resend-REST, FROM-Resolution, CC/BCC/Attachments, best-effort/wirft nicht; `src/lib/email/templates.ts` — Templates portiert + HTML-escaped, 6 Tests). Alle drei Flows direkt verdrahtet: **Invite** (`auth.ts createInvite`), **Portal-Login-Link** (`kunden.ts activate/resendPortalLink`), **Reservierungs-Bestätigung mit PDF** (`reservierungen.ts sendReservierungEmail` — Magic-Link jetzt in der Action statt Edge-Fn erzeugt, CC an VP, optional BCC). Edge-Functions bleiben im Repo (harmlos), werden aber nicht mehr aufgerufen. **Env (optional):** `EMAIL_FROM`/`RESEND_FROM_EMAIL` (globaler Default-Absender), `INVITE_FROM_EMAIL`, `PORTAL_FROM_EMAIL`, `RESERVATION_FROM_EMAIL`, `RESERVATION_BCC_EMAIL` — ohne diese wird Resends Testabsender `onboarding@resend.dev` genutzt (für Prod eine **verifizierte Domain** als Absender setzen). tsc + **219 Tests** + Build + Lint grün.

> **PROJ-6 Finanzierungen — Live-QA + Härtung (2026-06-18):** Nach der Reaktivierung (jetzt scharf erreichbar) fokussierter Pass auf die Finanzierungs-Actions (`src/lib/actions/finanzierung.ts`) — als einzige nicht im Security-Sweep-Batch. Alle Actions nutzen den **authed (RLS) Client** (kein Admin-Bypass → BUG-P1-Klasse n/a). RLS auf Prod verifiziert: `finanzierungs_cases` UPDATE durch zugewiesenen Finanzierer · owning VP/VL · admin/support, alle unter `org_isolation` (RESTRICTIVE). **Befund + Fix:** die UPDATE-Policy `case_vp_update` lässt auch den **VP** dieselbe Zeile schreiben (RLS kann nicht spaltenweise gaten) → ein VP hätte über `updateCaseOffer` die **finanzierer-exklusiven** Angebotsfelder (Zins/Rate/Summe) fälschen können. **Gehärtet:** `updateCaseOffer` jetzt `requireRole("finanzierer","admin","support")` (RLS `finanzierer_id = self` scopet zusätzlich auf den eigenen Case). `updateCaseStatus` bleibt RLS-gegatet (geteilter Workflow VP↔Finanzierer, keine pauschale Rollensperre ohne Business-Regel). tsc + 219 Tests + Build + Lint (0 Errors) grün. **BUG-F1 erledigt (2026-06-18):** Migration `20260618000000_revoke_anon_execute_finanzierung_rpcs` entzieht `anon` das EXECUTE auf den 4 direkten Finanzierungs-RPCs (`add/remove_finanzierer_from_pool`, `list_finanzierer_for_pool`, `get_my_kunde_cases`) — `authenticated` behält es (verifiziert anon=false/auth=true). Die 3 RLS-Prädikat-Helfer (`can_access_case`, `finanzierer_sees_einheit/_projekt`) bewusst NICHT angetastet (laufen in Policy-Auswertung). Ebenfalls erledigt: **robots-noindex** auf `/selbstauskunft/[token]` jetzt live verifiziert (war nur Cache). **Offen:** visuelle Prod-Verifikation als Finanzierer/VP.

> **Perf: getEinheitDetail entschlackt gegen statement_timeout/429 (2026-06-18, PROJ-3):** Nutzer meldete wiederkehrende `429` + `canceling statement due to statement timeout`. Diagnose: FK-Indizes (projekt_id/einheit_id auf bilder/dokumente/einheiten) sind **vorhanden** — Ursache ist RLS-Per-Row-Auswertung auf großen Result-Sets unter Contention. **Hebel 1:** `getEinheitDetail` lud bei *jedem* Aufruf alle **Geschwister-Einheiten (full select)** — die nur die **nie gemountete** nicht-eingebettete `EinheitDetailView` (GeschwisterCard) nutzte (`EinheitDetailView` wird ausschließlich `embedded` gerendert). Query + totes Feld entfernt (`geschwister: []`), `EINHEIT_SELECT` als toter Code raus → eine schwere RLS-Scan-Query pro Detail-Load weg. **Hebel 2:** doppeltes `getEinheitDetail` (Hero serverseitig + ProjektTabs client) entfernt — server-`initialDetail` wird durch ProjektTabs → `UnitDetailPane` durchgereicht und seeded den State (kein Refetch der initial gewählten Einheit). Netto: Projekt-Detailseite lädt das schwere Einheit-Detail **1× serverseitig** statt 2× (+ Geschwister-Scan weg) → weniger gleichzeitige Requests (429) und kürzere Query-Zeit (Timeouts). tsc + 219 Tests + Build + Lint (0 Errors) grün.

> **Ladezeit-Audit + Umsetzung (2026-06-22, cross-cutting PROJ-1/2/3/6):** Frischer Audit gegen `pg_stat_statements` (Prod) + Code. **Befund:** Der Projekt-first-Aggregat-Fix wirkt (`listProjekteUebersicht` günstig); **neuer Top-Kostenpunkt ist `getEinheitDetail` mit 26,3 % der gesamten DB-Exec-Zeit** (Ø 343 ms). **Wichtig (per EXPLAIN ANALYZE verifiziert):** Die Einheiten-PK-Query (0,8 ms) und die Bilder-`OR`-Query (2,4 ms) nutzen **bereits** alle Indizes sauber (`einheiten_pkey`, `idx_objekt_bilder_einheit/_projekt`) → die 343 ms sind **RLS-Auswertung + Free-Tier-Contention, KEIN fehlender Index**. Index-Tuning ist hier ein Sackgassen-Hebel. **Umgesetzt (Round-Trips/Bundle statt Index):** (1) **`getEinheitDetail`**: der teure eingebettete `projekte:projekt_id(...)`-**2-Tabellen-RLS-Embed-Join** (der dominante Kostenanteil des Hot-Paths) entfernt → `projekte` als parallele Single-Table-PK-Query ins bestehende `Promise.all` gezogen (kein Extra-Round-Trip, da `projekt_id` dort ohnehin gebraucht wird; richtet sich am bereits in `getProjektDetail` bewusst gewählten Muster aus). (2) **3 Server-Waterfalls parallelisiert:** `einstellungen/page.tsx` (Member-Liste), `objekte/projekt/[projektId]/page.tsx` (Deep-Link-Einheit `?einheit=` jetzt parallel zum Projekt — der extern geteilte Pfad; No-Param-Fall bleibt zwingend seriell), `finanzierungen/[caseId]/page.tsx` (Kommentare). (3) **6 `loading.tsx`-Skeletons** ergänzt (`/dashboard`, `/kunden`, `/team`, `/finanzierungen`, `/profil`, `/einstellungen`) — `/objekte`(+projekt) hatten sie schon. (4) **Bundle-Split:** `mapbox-gl` (~250 KB) + `recharts` (~100 KB) waren top-level in `ProjektDetailView`/`EinheitKalkulationPanel` → beide per `next/dynamic`(`ssr:false`) lazy: `HeroMap.tsx` (war schon extrahiert, jetzt verdrahtet) lädt Mapbox erst im Lage-Tab; neue `KalkulationCharts.tsx` isoliert recharts, `DiagrammePanel` ist nur noch dünner dynamic-Wrapper (der eager `useEinheitKalkulation`-Hook zieht recharts nicht mehr in den initialen Chunk). (5) **`next.config.ts`:** `optimizePackageImports: ['lucide-react','recharts','date-fns']`. **Nicht umgesetzt (bewusst):** `pg_timezone_names` (18,8 %, Ø 2,6 s) stammt **nicht aus App-Code** (kein Timezone-Query in `src/` — Studio/Tooling-Verbindung); Middleware-Auth-Skip auf Public-Routen (`proxy.ts` ruft `updateSession`/`auth.getUser` auch für `/start`,`/roadmap`,`/fuer`,`/selbstauskunft`) wäre ein echter Mikro-Hebel, ist aber eine **Auth-Flow-Änderung → Freigabe nötig** (security.md). tsc + Lint (0 Errors) + **219 Tests** + Build grün. **Offen:** Deploy auf Vercel (Server-Fixes wirken nur in Prod), danach Timing-Verifikation; Supabase-Compute-Upgrade bleibt der durable Schlussstein gegen Contention. **Deployed 2026-06-22** (Commit `cbcdb4c` → `main`/Vercel).

> **Staging-Environment + „The Loop" aufgesetzt (2026-06-22, cross-cutting):** Nutzerwunsch: testbare Staging-Umgebung, um den Define→Enumerate→Select→Simulate→Iterate→Real-life-test→Roll-out-Loop zyklen zu können (bisher ging jede Änderung direkt `main`→Vercel→Prod). **Entscheidungen:** Supabase **Pro** (25 $/Mo — löst zugleich die Free-Tier-Contention **und** schaltet **Branching** frei: ephemere DB pro Git-Branch) · **synthetischer Seed** (keine echte PII) · **manuell zuerst**, E2E-Gerüst vorbereitet. **Code-Unterbau gebaut (greift nach dem Pro-Upgrade):** (1) `docs/STAGING-PLAN.md` — Runbook mit Loop→Workflow-Mapping, Dashboard-Checkliste (Pro-Upgrade + Branching + Vercel-Supabase-Integration = **Nutzer-Schritte**, Billing/OAuth kann ich nicht auslösen), Demo-Logins, Phase-2-Plan (Migrations-Drift-Abgleich, da Repo-Migrationen unvollständig ggü. Prod sind → Branches reproduzieren ohne Abgleich nicht alle Policies wie `org_isolation`). (2) `supabase/seed.sql` — synthetischer Seed (1 Org, 3 login-fähige Demo-User admin/vp/finanzierer @staging.test PW `staging-demo-123`, 3 Projekte, 6 Einheiten, 3 Kunden); schema-verifiziert für Kern-Tabellen, defensiv (Upserts + Exception-Guards gegen Auth-Trigger/unbekannte `organisation_members`-Shape). (3) `supabase/config.toml` — `[db.seed]` aktiviert. (4) Cron `investagon-sync` gehärtet: skip bei `VERCEL_ENV != production` (nie gegen Branch-DB). (5) Playwright staging-fähig: `E2E_BASE_URL` steuert Ziel-URL (Preview/Prod statt nur localhost), `webServer` entfällt bei Remote; neuer `tests/smoke.spec.ts`. tsc + Lint (0 Errors) + Build grün. **Offen (Nutzer):** Phase 0 (Pro-Upgrade + Branching + Vercel-Integration im Dashboard). **Offen (ich, danach):** Phase 2 Migrations-Drift-Reconciliation + Seed gegen ersten echten Branch validieren.

| ID | Feature | Status | Spec | Created |
|----|---------|--------|------|---------|
| PROJ-1 | Auth, invites & role-based access (8 roles) | Deployed | — | 2026-06-11 |
| PROJ-2 | App shell (sidebar, topbar, command palette, notifications) | Deployed | — | 2026-06-11 |
| PROJ-3 | Objekte (projects/units, calculation, images, docs, map) | Deployed | — | 2026-06-12 |
| PROJ-4 | Kunden (pipeline, Bonität scoring, documents) | Deployed | — | 2026-06-12 |
| PROJ-5 | Reservierungen (signed PDF, expiry, email) | Deployed | — | 2026-06-11 |
| PROJ-6 | Finanzierungen (lender cases, offers, finanzierer pool) | Deployed (2026-06-18; Case-Anlage-Flow ergänzt: `requestFinanzierung` + „Finanzierung"-Reiter in der Kunden-Akte) | — | 2026-06-11 |
| PROJ-7 | Customer portal (dashboard, dokumente, Selbstauskunft, profil) | Deployed | — | 2026-06-11 |
| PROJ-8 | Präsentation / Exposé PDF generation | Deployed | — | 2026-06-11 |
| PROJ-9 | Provisionen (% vom Kaufpreis entlang VP-Hierarchie) + Team (Hierarchie, Sub-VP-Einladung, Provisionssätze) | Deployed (2026-06-18; BUG-P1 verifiziert gefixt — Flow end-to-end vollständig, kein Bauaufwand) | features/PROJ-9-provisionen-team.md | 2026-06-11 |
| PROJ-10 | Investagon API sync (mirror project/unit data into Supabase) | Deployed (Code auf `main`: UI-Trigger + Cron live; `CRON_SECRET` ist in Vercel-Env gesetzt ✓ — 2026-06-19 bestätigt) | docs/INVESTAGON-PLAN.md | 2026-06-11 |
| PROJ-11 | Domain structure audit (compare entity diagram vs. existing schema) | Deployed | — | 2026-06-11 |
| PROJ-12 | Objekte-Revision (expert feedback: simplified project mgmt, grouped fields, new fields, doc upload) | Deployed | docs/OBJEKTE-REVISION-PLAN.md | 2026-06-12 |
| PROJ-13 | Multi-Tenant Organisationen mit eigenem Branding + Org-Switcher (VP/Finanzierer) | In Progress (13.1+13.2 live) | docs/MULTI-TENANT-PLAN.md | 2026-06-12 |
| PROJ-14 | „Neue Objekte"-Feed (Partner-Feed, Schlagzeilen-Posts mit Projekt-Link) | Planned (V2, Nav ausgegraut, nicht gebaut) | features/PROJ-14-neue-objekte-feed.md | 2026-06-12 |
| PROJ-15 | Standort-Highlights-Modul (nächste POIs je Kategorie + Luftlinien-Distanz; Mapbox Search Box) | In Progress (MVP gebaut; Score & Isochronen out) | features/PROJ-15-standort-highlights.md | 2026-06-12 |
| PROJ-16 | Objekte anlegen — Bauträger raus + Bulk-Einheiten (Excel-Paste) | Deployed (2026-06-13; manuelle/E2E-Verifikation offen) | features/PROJ-16-objekte-bulk-anlegen.md | 2026-06-13 |
| PROJ-17 | Suchagenten (gespeicherte Suchprofile je Suchkunde, Auto-Match neuer/passender Einheiten, Benachrichtigung) | Roadmap | — | 2026-06-14 |
| PROJ-18 | Objektvergleiche (Side-by-side Vergleich von Einheiten/Kennzahlen, individuelle Portfolios) | Roadmap | — | 2026-06-14 |
| PROJ-19 | Externe Schnittstellen / APIs (CRM-Export & -Sync: HubSpot, Salesforce, TeamProQ; Webhooks) | Roadmap | — | 2026-06-14 |
| PROJ-20 | Kalkulation 2.0 (erweiterte AfA inkl. Denkmal/Möblierung, anpassbare Prognose-Szenarien, KfW-Förderbausteine) | Deployed (2026-06-15; `v1.20.0-PROJ-20`; visuelle Prod-Verifikation offen) | features/PROJ-20-kalkulation-2.md | 2026-06-14 |
| PROJ-21 | Einheiten-Vollständigkeit + Freigabe-Gate (voller Felddatensatz, Entwurf→Freigegeben nur bei allen Pflichtfeldern, „Fehlende Daten"-Ansicht, Bulk-Warnungen) | Deployed (2026-06-17, Commit `3f57e79`; inkl. UI-Entrümpelung der Freigabe-Karte; E2E/visuelle Prod-Verifikation offen) | features/PROJ-21-einheiten-vollstaendigkeit-freigabe.md | 2026-06-16 |
| PROJ-22 | KI-Lageeinschätzung + KI-Tags (füllt standort_highlights/tags, Exposé-/Präsentations-Lage) + Beratungs-Aufwertung | Deployed (2026-06-18; KI-Button via `getAiStatus` gegated. **Env-Blocker behoben:** Key liegt als `OPENAI_API_KEY` in Vercel ✓ — 2026-06-19 bestätigt; Client liest beide Namen. **Offen:** Live-KI-Verifikation als VP) | — | 2026-06-16 |
| PROJ-23 | Branding-Auto-Extraktion aus Website-URL (Onboarding + Einstellungen: URL → Logo/Primär-/Akzentfarbe automatisch, Vorschau + Übernahme) | Deployed (2026-06-18; Logo-Re-Hosting in Bucket + Create-Org-Logo-Passthrough ergänzt; editierbare In-Dialog-Vorschau offen) | features/PROJ-23-branding-auto-extraktion.md | 2026-06-17 |
| PROJ-24 | Personalisierte Lead-Sandbox („Branded Demo-Link") — Token-Link ohne Login öffnet eine interaktive, auf den Lead gebrandete Demo-Instanz mit generischen Musterobjekten (Akquise-Tool) | Deployed (2026-06-18; `/demo-links` Admin/Support + öffentliche `/fuer/[slug]`; Migration live; smoke-getestet auf Prod) | features/PROJ-24-lead-sandbox.md | 2026-06-18 |
| PROJ-25 | Design-System „Porsche-Struktur, Objektpilot-Marke" — Token-Architektur (Contrast-Stufen, Notification+Soft, State-, Spacing-, Motion-Tokens), A11y/Fokus-Disziplin von [Porsche DS v4](https://designsystem.porsche.com/v4/) übernommen; Navy/Gold-Marke + Inter behalten (Struktur statt Optik) | In Progress (Welle 1 umgesetzt: Token-Fundament + globale A11y/Motion + Cleanup AuthShell & ProjektDetailView; tsc/lint/219 Tests/Build grün) | docs/DESIGN-SYSTEM-PORSCHE.md | 2026-06-19 |
| PROJ-26 | KPI-/Pipeline-Dashboard (Vertriebs-Kennzahlen & Funnel: Lead→Reservierung→Finanzierung→Beurkundung, Conversion-Raten, Provisions-/Umsatz-KPIs, rollenabhängig VP/VL/Admin) | Roadmap (kein Spec) | — | 2026-06-19 |
| PROJ-27 | Link-Tracking im Beratungsprozess (Öffnungs-/Interaktions-Tracking für geteilte Links: Präsentation/Exposé, Selbstauskunft-Token-Link, Demo-Link — wer/wann geöffnet, Status im CRM, Benachrichtigung an VP) | Roadmap (kein Spec) | — | 2026-06-19 |
| PROJ-28 | Individuelle Präsentationen mit Steuervorteilen (Präsentation/Exposé individuell auf Objekt **und** Kunde gemünzt — persönl. Steuersatz/AfA-Effekt/Liquidität, Kalkulation-2.0-Engine je Kunde, personalisierte PDF) | Roadmap (kein Spec) | — | 2026-06-19 |
| PROJ-29 | KI-Objektmatching & -vergleich (KI matcht passende Einheiten zu Kunde/Suchprofil + KI-gestützter Side-by-side-Vergleich/Begründung; baut auf PROJ-17 Suchagenten + PROJ-18 Objektvergleiche) | Roadmap (kein Spec) | — | 2026-06-19 |
| PROJ-30 | Org-Custom-Domains (App kanonisch auf Vercel-URL; jede Org verbindet eine eigene Domain via Vercel-API → Host→Org-Auflösung, org-gebrandete App; emi-hub.de → Haupt-Org) | In Progress (DB-Fundament + emi-hub-Mapping live; Vercel-Client/Host-Auflösung/UI offen) | features/PROJ-30-org-custom-domains.md | 2026-06-22 |

<!-- Add features above this line -->

> **Design-System-Vorschlag + Post-Redesign-Roadmap (2026-06-19, PROJ-25 + offene Features):** Nutzerwunsch: neues Design-System „von [Porsche DS v4](https://designsystem.porsche.com/v4/) übernommen" + danach „welche Features hieraus noch offen sind, alle in PROJects planen". **Entscheidung des Nutzers:** Porsche **Struktur statt Optik** übernehmen · **Plan zuerst** (kein Code) · **Navy/Gold-Marke + Inter behalten**. **PROJ-25 angelegt** (Vorschlag liegt in `docs/DESIGN-SYSTEM-PORSCHE.md`): verfeinert das bereits vorhandene OKLCH-/shadcn-Token-System um Porsche-Konventionen (Contrast-Stufen `low/medium/high`, Notification `+ -soft`, State-Tokens `hover/active/focus`, benannte Spacing-/Motion-Skala, globale `:focus-visible`-/A11y-Disziplin), phasiertes risikoarmes Rollout (Tokens → Primitives → A11y → Konsolidierung). Kein Porsche-Rot, keine Web-Components, kein Re-Layout. Offene Entscheidungen im Doc: Button-Radius (Pill vs. `rounded-md`), Dark-Mode aktiv?, Umgang mit „B1 Klar"-Editorial (Spectral/Paper). **Sinnvolle Sequenz:** PROJ-25 (Design-System-Fundament) zuerst, danach die Feature-Arbeit auf dem neuen System.
>
> **Noch offene Features (Stand 2026-06-19, „alle in PROJects geplant"):**
> - **PROJ-13** Multi-Tenant Org + Branding/Org-Switcher — *In Progress* (13.1+13.2 live). **Offen:** restliche Sub-Schritte gem. `docs/MULTI-TENANT-PLAN.md` (Org-Switcher-Politur, vollständige Branding-Vererbung). → kein neuer Spec nötig, Plan-Doc existiert.
> - **PROJ-14** „Neue Objekte"-Feed (Partner-Feed, Schlagzeilen-Posts mit Projekt-Link) — *Planned* (V2, Nav ausgegraut, ungebaut). Spec da (`features/PROJ-14-neue-objekte-feed.md`). → bereit für `/architecture` → `/frontend`+`/backend`, sobald priorisiert.
> - **PROJ-15** Standort-Highlights — *In Progress* (MVP live). **Offen:** Score & Isochronen (bewusst zurückgestellt) + canonical_ids mit echtem Mapbox-Token verifizieren (v. a. `motorway_junction`). Spec da.
> - **PROJ-17** Suchagenten (gespeicherte Suchprofile, Auto-Match, Benachrichtigung) — *Roadmap*, **kein Spec**. → nächster Schritt `/write-spec PROJ-17`.
> - **PROJ-18** Objektvergleiche (Side-by-side Kennzahlen, Portfolios) — *Roadmap*, **kein Spec**. → `/write-spec PROJ-18`.
> - **PROJ-19** Externe Schnittstellen / APIs (CRM-Sync HubSpot/Salesforce/TeamProQ, Webhooks) — *Roadmap*, **kein Spec**. → `/write-spec PROJ-19`.
> - **Deployed, aber Verifikation offen** (kein Bauaufwand, nur Validierung): PROJ-3/8/16/20/21/22/23/24 haben jeweils „visuelle Prod-Verifikation / E2E offen"; PROJ-10 braucht `CRON_SECRET`, PROJ-22 braucht `OPEN_API_KEY` in Vercel-Env.
>
> **Empfohlene Reihenfolge:** (1) PROJ-25 Design-System-Fundament umsetzen (Freigabe der offenen Entscheidungen nötig), (2) Specs für PROJ-17/18/19 via `/write-spec`, (3) PROJ-14 architekturieren+bauen, (4) PROJ-15 Score/Isochronen + PROJ-13-Restschritte. Bauen erst nach Freigabe je Feature (Human-in-the-loop).
>
> **Nachtrag 2026-06-19 (Nutzer: „es fehlen noch Dinge"):** Vier zusätzliche Features als Roadmap-PROJ angelegt — **PROJ-26 KPI-/Pipeline-Dashboard**, **PROJ-27 Link-Tracking im Beratungsprozess** (inkl. Selbstauskunft-/Präsentations-/Demo-Link-Öffnungen → Status im CRM + VP-Benachrichtigung), **PROJ-28 Individuelle Präsentationen mit Steuervorteilen** (Objekt **+** Kunde, persönl. Steuersatz/AfA via Kalkulation-2.0-Engine → personalisierte PDF), **PROJ-29 KI-Objektmatching & -vergleich** (baut auf PROJ-17/18). Alle noch **ohne Spec** → nächster Schritt je `/write-spec PROJ-26…29`. **„Standort-Highlights mit KI"** ist bereits durch **PROJ-22** abgedeckt (KI füllt `standort_highlights`/`tags`) — mit gesetztem Key jetzt live nutzbar; PROJ-15 verweist darauf, eigener Build nur für Score/Isochronen nötig. **Env-Blocker behoben (Nutzer bestätigt):** `OPENAI_API_KEY` ✓ (PROJ-22 live-fähig) und `CRON_SECRET` ✓ (PROJ-10 Cron aktiv) sind in Vercel gesetzt.
>
> **PROJ-25 Umsetzung Welle 1 (2026-06-19):** Nutzer: „Baue das Design in die komplette App ein — kein KI-Look, clean, wenig komische Extra-Buttons, leane Userführung." **Umgesetzt:** (1) **Token-Fundament** in `globals.css` + `tailwind.config.ts` — Contrast-Stufen (`text-contrast-high/medium/low`), Notification-Soft-Flächen (`bg-success/warning/destructive/info-soft`), State-Tokens (`bg-state-hover/active`), benannte Spacing-Skala (`xs..3xl`), Motion-Tokens (`duration-ds-*`, `ease-ds*`) — additiv, bestehende Tokens bleiben als Aliase → **null Breaking**. (2) **Globale A11y/Motion** — `:focus-visible`-Fallback für nackte Links/Tab-Targets + `prefers-reduced-motion`-Reset. (3) **Cleanup `AuthShell`** — dekorativer Radial-Gradient-Blob + Marketing-Eyebrow „Vertriebsplattform" raus → ruhige Login-Fläche. (4) **Cleanup Flaggschiff `ProjektDetailView`** — drei tote Deko-Rundbuttons (Merken/Teilen/Mehr, ohne Handler) entfernt, toter „Preis vorschlagen"-Button entfernt, Champagne-Pill um den Kaufpreis entfernt; nur funktionale Aktionen bleiben. tsc + Lint (0 Errors) + **219 Tests** + Build grün. **Welle 2 — tote-Buttons-Sweep (2026-06-19, erledigt):** App-weiter Scan aller rohen `<button>`/`<Button>` in 21 View-Dateien (Explore-Agent + manuell verifiziert). Befund: App ist weitgehend sauber — nur **zwei** echte tote Buttons, beide **verdrahtet statt entfernt** (leane Userführung): MobileTabbar-„Neuer Kunde"-FAB → Link `/kunden/neu`; Topbar-„Neuer Sub-VP" → Link `/team` (wo der Einladungs-Dialog lebt). tsc + Lint grün. **Offen (Welle 3, optional):** Primitives auf State-/Contrast-/Notification-Soft-Tokens umstellen (Button/Input/Card/Badge), dann Konsolidierung der doppelten `brand.*`-Hex.

> **Lage-Highlights im Lage-Tab + Lightbox-Fix (2026-06-19, PROJ-3/PROJ-22):** (1) Der **Lage-Tab** (`ProjektTabs`) zeigt jetzt zusätzlich die **Freitext-Lage-Highlights** (Feld `standort_highlights`, KI/manuell) — bisher nur im Einheit-Detail sichtbar. Projektweit gelöst: alle Einheiten teilen die Adresse → erster gepflegter Text wird gezeigt (`LageHighlightsText`, `SectionCard` „Lage-Highlights"). `standort_highlights` neu in `ObjektListItem` + `mapEinheitRow`, aber **nur** im Projekt-Detail-Query geladen (`${EINHEIT_LIST_COLS}, standort_highlights`), nicht in der globalen Einheiten-Liste → deren Payload bleibt schlank. (2) **Bildansicht-Bug behoben (war „extrem buggy"):** `ImageLightbox` (`fixed inset-0`) wurde **innerhalb** des gestylten `GalleryHero` gerendert; ein `transform`/`filter`-Vorfahr macht aus einem `position:fixed`-Element einen Containing-Block → das Overlay wurde mitten auf der Seite abgeschnitten/verschwommen statt Vollbild (genau das Screenshot-Symptom). **Fix:** Overlay via `createPortal` an `document.body` → immer viewport-fix, unabhängig von Vorfahren-Styles; behebt alle `ImageLightbox`-Nutzungen. (`ProjektGalerie` hat eine eigene Inline-Lightbox, ist aber toter Code — nicht gerendert, daher nicht angefasst.) tsc + 219 Tests + Build + Lint (0 Errors) grün. **Deployed 2026-06-19** (Commit `b19455b` → `main`/Vercel). **Offen:** visuelle Prod-Verifikation (eingeloggter VP).

> **504-/Ladezeit-Pass (2026-06-19, cross-cutting PROJ-1/2/3):** Nutzer meldete anhaltende Ladezeiten + flächendeckende **HTTP 504** (`/dashboard`, `/objekte`, `/team`, `/profil`, `/einstellungen`, `/demo-links`, `/objekte/projekt/[id]` — viele davon als `?_rsc=`-Prefetches). **Diagnose an Prod-DB:** Postgres-Logs zeigen eine `profiles`-Abfrage **über den PK** mit **10.276 ms** + reihenweise `canceling statement due to statement timeout`; selbst Admin-`execute_sql` und der Performance-Advisor liefen beim Verbinden in Timeout → die **Free-Tier-Instanz war gesättigt** (Haupt-Root-Cause; Tarif-Upgrade vom Nutzer vorerst abgelehnt). **Code-Gegenmaßnahmen umgesetzt (B/C/D ohne RLS-Eingriff):** (1) **Prefetch-Sturm gekappt** — `prefetch={false}` auf Sidebar-Nav + Logo (`AppSidebar`), Projekt-Karten (`ProjektCard`) und `MobileTabbar`; jeder Link-Prefetch hatte zuvor die volle Middleware-`auth.getUser()` + Layout-Query-Kette gegen die DB neu ausgelöst (= die `?_rsc=`-504-Flut). `loading.tsx`-Skeletons decken die Klick-Navigation ab. (2) **Layout-Roundtrips reduziert** — `getActiveOrganisation` macht statt zweier serieller Queries (profiles→organisationen) **eine** eingebettete Abfrage über `profiles_active_organisation_id_fkey`; Root-Layout holt Org-Branding + Session via `Promise.all` (geteilter `cache()`-`getSessionUser`, 1 Auth-Roundtrip). (3) **Client-`user_roles`-Fetch entfernt (D)** — Rollen werden jetzt vom Server (`getSessionUser`) in `Providers`→`AuthProvider` geseedet (`initialRoles`/`initialUserId`); der Browser fragt `user_roles` nur noch, wenn sich der eingeloggte User vom server-gerenderten unterscheidet → beseitigt die **CORS**-Konsolen-Fehler (`user_roles?select=role…`) **und** eine DB-Query pro Seitenaufruf. **Bewusst NICHT angefasst:** die schwere `profiles`-RLS (~290 InitPlans) — die 10 s waren Instanz-Sättigung, nicht der RLS-Filter eines 1-Zeilen-PK-Lookups; eine RLS-Vereinfachung ist mandanten-sicherheitskritisch und braucht eine erreichbare DB zum Verifizieren. tsc + Lint (0 Errors) + **219 Tests** grün. **Offen:** Deploy auf Vercel (Fixes wirken nur in Prod), danach 504-/Timing-Verifikation; Supabase-Compute-Upgrade bleibt der direkteste Hebel gegen erneute Sättigung. (Browser-Extension-Rauschen `frame_ant.js`/`kwift` + CSS-`preload`-Warnung sind nicht App-bezogen.)

> **Perf-Tuning Caching-Layer (2026-06-19, Forts. 504-Pass, cross-cutting):** Nach dem 504-Pass + DB-Reboot den Skill-„supabase-performance-tuning"-Ansatz angewandt. **Befund:** App nutzt ausschließlich PostgREST (HTTP) — **keine** direkten PG-Verbindungen (`pg`/Pool/`:6543`/`:5432` = 0 Treffer), daher ist Skill-Schritt 4 (PgBouncer/Pooling) kein App-seitiger Hebel (Supabase-Infra macht das). **Umgesetzt (Caching, Schritt 2):** `getActiveOrganisation` (läuft im Root-Layout = jeder Seitenaufruf) holt das Org-Branding jetzt aus einem **cross-request `unstable_cache`** (`getOrgBrandingCached`, 60 s TTL, Service-Client-Read nur der nicht-geheimen Branding-Felder) → die `organisationen`-Query **inkl. ihrer RLS** fällt für ~alle Requests aus dem Hot-Path (warm = 0 DB). Die `active_organisation_id` des Users wird self-scoped über den Admin-Client gelesen (by authenticated `userId`) → umgeht zusätzlich die schwere `profiles`-RLS (290-InitPlan-Policy, im Log mit 10 s) auf dem Hot-Path; es überschreitet keine Mandantengrenze (nur eigene Zeile). **Invalidierung:** TTL-basiert (Branding ändert sich ~einmal bei Org-Setup; Settings-UI zeigt neue Werte sofort aus dem Action-Returnwert, nur die Layout-Chrome lagt ≤60 s). Next 16 `revalidateTag` (2-arg, neues `'use cache'`-System) bewusst nicht genutzt — `'use cache'` zu aktivieren wäre ein breiter Config-Eingriff, TTL ist hier der sichere, von der Skill sanktionierte Weg. Batching/DataLoader n/a (kein GraphQL, N+1 im Layout durch den Cache erledigt). tsc + Lint (0 Errors) + 219 Tests + Build grün. **Offen:** Deploy; nach DB-Recovery Timing-Verifikation; Compute-Upgrade bleibt der durable Schlussstein.

> **Investagon-Feature-Gap-Analyse + PROJ-17–20 (2026-06-14/15):** Vollständiger Abgleich der App gegen Investagons Marketing-Featureliste durchgeführt (Code-verifiziert). Kern-DNA vorhanden (Objekte, Exposé, Kalkulation+Charts, CRM/Bonität, Reservierung, White-Label = gleichauf/besser). Neu auf Roadmap: **PROJ-17 Suchagenten**, **PROJ-18 Objektvergleiche**, **PROJ-19 externe Schnittstellen** (HubSpot/Salesforce). Verworfen: Glossar, Notar-Koordinations-Workflow. Offen als Lücken: Wartelisten, echtes Marktdaten-Modul. **PROJ-20 Kalkulation 2.0 GEBAUT** (2026-06-15): erweiterte AfA (Denkmal §7i mit Substanz-Split, Sonder-AfA §7b, Möblierungs-AfA), 3 umschaltbare Prognose-Szenarien (konservativ/individuell/historisch) inkl. Inflation auf Kosten, KfW als zweite Darlehenstranche mit Tilgungszuschuss; Engine rückwärtskompatibel (neue Felder optional), Präsentation defaultet konservativ. `src/lib/kalkulation.ts` + `KalkulationsTab.tsx` + `PraesentationView.tsx`, 16 neue Tests. tsc + 171 Tests + Build grün. **QA-Pass 2026-06-15:** keine Critical/High; fachliche Korrektheit (§7i 12-J-Summe = 100 %, KfW-Split, Inflation) verifiziert; BUG-K1 (KfW-Dropdown kosmetisch) direkt gefixt. **Deployed 2026-06-15** auf https://objekt-pilot.vercel.app (Commit `6347ef1`, Tag `v1.20.0-PROJ-20`, Smoke-Test 200/307). **Offen:** visuelle Prod-Verifikation des Kalk-Tabs durch eingeloggten VP, E2E (braucht Staging+Seed), optionale DB-Persistenz der Inputs.

> **PROJ-7 Selbstauskunft-OHNE-Login (Token-Link) — Security-Audit (QA 2026-06-15):** Red-Team-Pass der **unauthentifizierten** Fläche `/selbstauskunft/[token]` + `src/lib/actions/selbstauskunft-public.ts` (alle Reads/Writes via Service-Role-Client, strikt auf den Token-Kunden gescopt). **Solide Basis:** Token = `kunden.selbstauskunft_token` DEFAULT `gen_random_uuid()` (UUIDv4, 122 Bit, NOT NULL, ≠ `id`) → nicht brute-forcebar; **kein IDOR** (kein client-gelieferter `kunde_id` wird vertraut, Immobilien-Subform an `selbstauskunft_id` des Token-Kunden gebunden); IP wird vor `inet`-Cast via `sanitizeIp()` validiert; Invalid-Token → generische Meldung (kein Oracle); Token nur dem verwaltenden VP sichtbar (`KundeDetailView`); die `submit_selbstauskunft`-RPC (Advisor-WARN „anon-executable") ist die **authentifizierte** Variante und fail-safe (`where user_id = auth.uid()` → für anon leer → RAISE). **Keine Critical/High.** **Findings:** (1) **BUG-S1 (Medium)** — keine serverseitige Immutability/Expiry: `submitSelbstauskunftByToken` akzeptiert & überschreibt eine bereits `eingereicht`-e (unterschriebene) Selbstauskunft erneut (`upsert onConflict kunde_id`, nur Client-Flag `alreadySubmitted`); Token läuft nie ab / ist nicht Single-Use. Ein zurückbehaltener/geleakter Link erlaubt Manipulation eines rechtlich unterschriebenen Finanz-Dokuments + stilles Neuberechnen der Bonität (`max_finanzierbar` etc.). Empfehlung: Submit bei `status='eingereicht'` ablehnen oder versionieren; Token nach Submit rotieren/expiren. (2) **BUG-S2 (Low)** — Public-Token-Seite ohne `robots: noindex`/`X-Robots-Tag`; Magic-Link-URLs sollten nicht indexierbar sein. (3) **BUG-S3 (Low/Obs.)** — kein Rate-Limit auf den Public-Actions (Token-Brute-Force dank UUIDv4 unrealistisch; ein Token-Inhaber kann aber Writes spammen). Tests: 29 grün (`selbstauskunft` + `bonitaet`). E2E n/a (lokal kein Token-Seed). Status bleibt **Deployed** (Medium blockiert nicht, aber Fix empfohlen).

> **PROJ-6 Finanzierungen — Security-Audit (QA 2026-06-15):** PROJ-6 bleibt **Deferred (V2)**, kein Spec-File → kein Akzeptanzkriterien-Durchlauf; stattdessen Red-Team-Audit der **noch lebenden Server-Actions** (`src/lib/actions/finanzierung.ts`) + DB-Layer. Beide Routen (`/finanzierungen`, `/finanzierungen/[caseId]`) sind soft-gated auf `FeatureComingSoon` (kein Daten-Leak via UI), aber die `"use server"`-Actions sind weiterhin per POST aufrufbar — Schutz liegt allein auf RLS/RPC. **Befund: RLS korrekt geschichtet** — `finanzierungs_cases` kombiniert PERMISSIVE Granular-Policies (admin/support · zugewiesener Finanzierer · VP-Owner/Subtree/VL) **AND** RESTRICTIVE `org_isolation` (Mandanten-Guard). Ergebnis: Kunde kann **weder SELECT noch UPDATE** auf Cases (auch die 2 Kunden mit `active_organisation_id IS NULL` nicht — sie scheitern an der Permissive-Ebene; eigene Cases nur über die gescopte `get_my_kunde_cases`-RPC). Pool-RPCs `add/remove_finanzierer_from_pool` sind SECURITY DEFINER mit explizitem `admin/support`-Gate (RAISE 42501; für `anon` fail-safe da `auth.uid()` NULL). `can_access_case` granular. **Keine Critical/High/Medium.** Advisors: **0 ERROR**, 55 WARN. **Findings (beide Low):** (1) **BUG-F1** — 16 `*_security_definer_function_executable`-WARN: SECURITY-DEFINER-RPCs der Finanzierungs-Familie sind für `anon` EXECUTE-bar; nicht ausnutzbar (interne Rollen-/`auth.uid()`-Checks greifen fail-safe), aber Hygiene: `REVOKE EXECUTE … FROM anon` auf internen Helper-Prädikaten (`can_access_case`, `finanzierer_sees_*`) empfohlen → `docs/USER-TODO.md`. (2) **Observation** — `updateCaseOffer`/`updateCaseStatus` verlassen sich rein auf RLS (kein Code-Rollen-Check); für ein bewusst zurückgestelltes Feature wäre Defense-in-Depth (Feature-Flag/Rollen-Gate auf Action-Ebene + expliziter Rollen-Check) sauberer. Tests: 9/9 (`portal-finanzierung-hint`) + Gesamt-Suite 171 grün. E2E n/a (UI gegated). Status bleibt **Deferred**.

> **Objekte-Revision Runde 2 (Refine 2026-06-12, PROJ-3/PROJ-12):** Projekt-Detailseite (`/objekte/projekt/[projektId]`) mit Kaufpreisliste + Einheiten-Anzahl + Verkaufsstatus-Breakdown geplant; Status-Taxonomie auf strikt 6 Werte (Frei · Auf Anfrage · Reserviert · Notarvorbereitung · Notartermin · Verkauft). Listen-Aggregation/Ranges existieren bereits. Plan: `docs/OBJEKTE-PROJEKT-DETAIL-PLAN.md`. Noch nicht gebaut.

> **Backlog (für `/goal`-Session, 2026-06-12):** (1) ~~**UI-Politur-Pass**~~ **✓ erledigt 2026-06-12** — „Vertriebsplattform"-Marketing-Pills aus Objekte-Header (`ObjekteListView.tsx`) & Dashboard (`dashboard/page.tsx`) entfernt, dekorativer Akzent-Strich unter „Objekte" entfernt, 👋-Emojis aus Kundenportal (`PortalDashboardView.tsx`, `PortalShell.tsx`) entfernt. Funktionale Elemente (Benachrichtigungs-/Timeline-Dots, Lade-Skeletons, dezente Karten-Hover, „Bald"-Gating) bewusst belassen — professionell üblich. Typecheck grün. (2) ~~**PROJ-15 Standort-Highlights**~~ **✓ MVP gebaut 2026-06-12** — Mapbox Search Box Category Search, 5 Kategorien (ÖPNV/Einkauf/Bildung/Gesundheit/Autobahn), nächster POI je Kategorie + Luftlinien-Distanz, im Einheiten-Karte-Tab & in der Präsentation (`SlideLage`-Platzhalter ersetzt). Score & Isochronen bewusst out. Spec: `features/PROJ-15-standort-highlights.md`. **Offen (siehe Spec Open Questions):** canonical_ids mit echtem Token live verifizieren (lokal nur Dummy-Token), v. a. `motorway_junction`.

> **PROJ-10 Echtdaten-Sync (2026-06-12):** Investagon-Sync auf **echte Daten** umgestellt. Befund: Die `Api*`-Listen-Endpoints liefern nur Adresse+Status — die echten Finanz-/Struktur-/Medien-Daten liegen auf den **vollen** `Project`/`Property`-Ressourcen (`GET /api/projects/{id}`, `/api/properties/{id}`; 48 bzw. 114 Felder). Der frühere synthetische Generator (Zufalls-Finanzen) ist **entfernt**. Neu: `fetchFullProject/fetchFullProperty` im Client; `sync-core` mappt echte Felder (Wohnfläche, Zimmer, Etage, Kaufpreis, Miete, Energieklasse, Heizung, Zustand, vermietet …), reichert Projekt-Adresse/Geo aus erster Einheit an und synct Fotos→`objekt_bilder` + Dateien→`objekt_dokumente` (idempotent, CDN-URL-scoped). Live verifiziert an 1 Projekt (Halle, 76 Einheiten, 20 Fotos) — echte Werte korrekt. **Volllauf erledigt (2026-06-12):** 222 Projekte, 2108 Einheiten, 7023 Fotos, 9703 Dokumente in 155,8 s in die Prod-DB synchronisiert (2 Projekt-404s graceful abgefangen). **UI-Trigger gebaut:** Karte „Investagon-Synchronisierung" in Einstellungen (admin/support), zeigt letzten Sync-Status und startet einen **inkrementellen** Abgleich (`syncInvestagon({incremental:true})`, `updated_after` seit letztem Erfolg − 1 h Overlap) — Volllauf bleibt das Seed-Skript (Vercel-Timeout). Neue Actions: `getInvestagonStatus`, `syncInvestagon(incremental)`. **Offen:** Deploy auf Vercel; optional Cron für inkrementellen Sync. Details: `docs/INVESTAGON-PLAN.md`.

> **PROJ-8 Präsentation QA + Aufwertung (2026-06-12):** QA-Befunde gefixt: (1) Mietrendite zeigte überall „—" (Projekt-Wert nach Echtdaten-Sync null) → wird jetzt aus Miete·12/Kaufpreis berechnet; (2) hartcodierte Falschaussage „Vollvermietung mit stabilem Cashflow" wurde auch bei Leerstand gezeigt (rechtlich heikel) → durch datengetriebene Objektfakten ersetzt; (3) beliebiges Foto wurde als „Grundriss" gelabelt → nur noch bei echtem Grundriss, sonst „Ansicht"; (4) Hero nutzt verlässlich `cover_image_url`. Aufwertung: Cover-Kennzahlen-Strip (Preis·Fläche·Zimmer·Rendite), Übersicht zeigt echte Sync-Daten (Baujahr, Zustand, Energieklasse, Heizung, Stellplatz, Vermietung) mit Icons, Eckdaten + Stellplatzpreis. Nur `PraesentationView.tsx`. tsc + 113 Tests grün. **Offen:** visuelle Verifikation auf Staging/Prod (Mapbox-Token lokal nur Dummy).

> **PROJ-7 Kundenportal QA + Aufwertung (2026-06-12):** QA-Befunde gefixt: (1) Dashboard-Kachel „Unterlagen" zeigte **immer 0** (hartcodiert) → echter Stand aus `kunden_dokumente` (nötige Kategorien je `beruf_status` vs. tatsächlich hochgeladene); (2) Status-Badges rendern **rohe Enum-Werte** („frei", „reserviert") → kundenfreundliche Labels via `statusLabel()`; (3) Wohnungskarten zeigten Platzhalter „Details folgen in Kürze" → echte Eckdaten (Wohnfläche · Zimmer · Kaufpreis). Datenebene `getPortalDashboard` um `dokumentKategorien` + Einheit-Eckdaten erweitert. Nur `PortalDashboardView.tsx` + `data/portal.ts`. tsc + 113 Tests grün. **Offen:** „Mein Status"-Seite ist noch ein Platzhalter — echte Status-Pipeline (Reservierung→Selbstauskunft→Bonität→Finanzierung) wäre der nächste Aufwertungsschritt.

> **Autonome Bau-Session (2026-06-12) — V1-Stubs verdrahtet:** Mehrere „In Vorbereitung"/TODO(migration)-Stubs an echte Daten angebunden. **PROJ-2 (App-Shell):** (1) `NotificationBell` lädt jetzt echte `notifications` (Server-Action `listMyNotifications`/`markNotificationsRead`, optimistic); (2) `FeedbackButton` persistiert in `feedback` (neue Action `submitFeedback`, Kategorie-Select); (3) `CommandPalette` mit echter, RLS-gescopter Suche über Einheiten/Projekte/Kunden (Action `searchEntities`, debounced). **PROJ-7 (Portal):** (4) „Mein Status" ist jetzt eine echte **Status-Pipeline** (`PortalStatusView`: Wohnung→Selbstauskunft→Bonität→Reservierung→Finanzierung→Beurkundung, aus echten Daten abgeleitet, Fortschritts-% + „Jetzt dran"); (5) „Nachrichten" zeigt jetzt den echten Benachrichtigungs-Feed des Kunden (`PortalNachrichtenView`, Empty-State + Kontakt-Hinweis; echter 2-Wege-Chat bleibt späteres Feature). Hinweis: Projekt-Detailseite (`/objekte/projekt/[id]`) + 6-Werte-Status-Taxonomie waren **bereits gebaut** (Plan-Doc `OBJEKTE-PROJEKT-DETAIL-PLAN.md` war veraltet). tsc + 113 Tests + `npm run build` grün.

> **Autonome Bau-Session Wave 2 (2026-06-12):** Weitere V1-Stubs verdrahtet. **PROJ-3 (Objekte):** (1) `BankDatenCard` ist jetzt echtes Lesen/Schreiben der Projekt-Bankdaten (IBAN/BIC/Kontoinhaber) via Actions `getProjektBank`/`updateProjektBank` (authed Client → RLS-gescopt, IBAN/BIC-Validierung); (2) `CompletenessCard` `bank`-Signal jetzt ehrlich aus `projekte.bank_iban` (Datenebene `EinheitDetail.bank_complete`) statt hartcodiert false. **PROJ-10 (Sync):** (3) Vercel-Cron `GET /api/cron/investagon-sync` (täglich 04:00, `vercel.json`) — inkrementeller Sync aller Orgs mit Creds, geschützt per `CRON_SECRET`, überspringt Orgs ohne vorherigen Erfolgslauf (kein Timeout). `CRON_SECRET` muss in Vercel-Env gesetzt werden (Doku: `docs/INVESTAGON-PLAN.md`). tsc + `npm run build` grün.

> **Autonome Bau-Session Wave 3 (2026-06-12) — Hygiene:** (1) Lint-Status korrigiert (war fälschlich als „defekt" notiert; läuft mit 0 Errors); CommandPalette-Reset sauberer (über `onOpenChange` statt Effect); (2) toter `KarteStub` + ungenutzter `MapPin`-Import aus `EinheitDetailView` entfernt; (3) USER-TODO Investagon-Notiz auf „echte Daten erledigt" aktualisiert.

> **Selbstauskunft & Reservierung — Fillout-Nachbau (Plan 2026-06-13, PROJ-7/PROJ-5):** Bestehende Fillout-Formulare (`Selbstauskunft.json`, `Reservierung EMI.json` im Root) als App-Formulare **im Kundenportal** nachbauen, Unterschrift via Zeichnen (`SignaturePad`, kein E-Signing). Selbstauskunft = 8 Schritte inkl. Mitantragsteller, bedingte Felder, Immobilien-Subform. Reservierung erweitert um Steuer-ID/IBAN/Staatsangehörigkeit/Mitantragsteller/Bestätigungen. **Bauen pausiert — wird in frischem Chat ausgeführt.** Exakte Feldliste: `docs/FILLOUT-FORMSPEC.md`; vollständiger Plan: `docs/RESERVIERUNG-SELBSTAUSKUNFT-PLAN.md`. Read-only Selbstauskunft-Tab (VP-Sicht) ist bereits live (`5051c28`). Offen: Immobilien-Subform-Spalten, Migration/RLS-Freigabe.

> **Bau-Session 2026-06-13 — PROJ-16 + Fillout-Nachbau (PROJ-7/PROJ-5) umgesetzt:**
> **PROJ-16** (Objekte anlegen): Bauträger aus Eingabe/Anzeige/Suche entfernt (DB-Spalte bleibt für Sync); neuer **Bulk-Einheiten-Editor** mit Excel-Paste + Spalten-Mapping + de-Zahlenparsing (`EinheitenBulkGrid`, lib `objekte-bulk.ts` 24 Tests, Action `createEinheitenBulk`); im mfh-Wizard Standard mit Umschalter „Einzeln mit Details". **Fillout-Nachbau** (PROJ-7/PROJ-5): Migration auf Prod (Tabellen `selbstauskuenfte` + `selbstauskunft_immobilien`, `reservierungen`-Erweiterung, RLS analog `kunden`, 0 ERROR-Advisors); neuer **8-Schritt-Selbstauskunft-Wizard** im Portal (Mitantragsteller, bedingte Felder, Immobilien-Subform, Autosave, Unterschrift, Prefill aus URL/CRM+DB) ersetzt den alten 5-Schritt; Domänen-lib `selbstauskunft.ts` (15 Tests) + Actions (get/save/submit, kunden-Mirror + konservative Netto-Bonität via Admin-Client). **Reservierung** um Steuer-ID/IBAN/Staatsangehörigkeit/Mitantragsteller/Bestätigungen/Ort/Datum erweitert (Action + Modal + PDF), Prefill aus Selbstauskunft. tsc + **156 Tests** + `npm run build` grün. QA bestanden (keine Critical/High; BUG-Q1–Q4 gefixt). **Deployed 2026-06-13** auf https://objekt-pilot.vercel.app (Vercel-Build „Ready", Smoke-Test 200; Commits `a21c7ba` + `a8cc531`). **Offen:** manuelle/E2E-Verifikation auf Prod (Portal-Flow + Bulk-Grid mit echter Excel-Liste); Close-URL-Param-Namen verifizieren. Details: `docs/RESERVIERUNG-SELBSTAUSKUNFT-PLAN.md`, `features/PROJ-16-objekte-bulk-anlegen.md`.

> **Projektseite konsolidiert (2026-06-13, PROJ-3, deployed `52ac62f`):** LearningSuite-inspirierter Umbau — alles auf der Projektseite, **keine komplizierte Einheiten-Unterseite** mehr. Cleaner Hero (Galerie + Zusammenfassungs-Karte + Kennzahlen-Strip), darunter **Pill-Tabs** (Einheiten · Lage · Admin: Bankdaten/Pool). Tab „Einheiten" = **Master-Detail**: Liste + Verkaufsstatus links, Detail der gewählten Wohnung rechts (lazy via `getEinheitDetailAction`, volle Funktionen über `EinheitDetailView` mit neuem `embedded`-Modus). `/objekte/[einheitId]` leitet auf `/objekte/projekt/[id]?einheit=…` weiter (Deep-Links bleiben gültig). tsc + build grün, live, Smoke-Test 200. **Offen:** visuelle Politur am Detail-Pane (die Einheit-Sub-Tabs enthalten noch Karte/Bankdaten, leicht redundant zu Projekt-Tabs — optional trimmen).

> **Nav-Bereinigung + PROJ-3-Trim (2026-06-13, deployed `8508da0`):** **Finanzierungen** (PROJ-6) und **Provisionen** (PROJ-9) jetzt **komplett ausgeblendet** statt ausgegraut („Bald") — neues `hidden`-Flag in `src/lib/navigation.ts` entfernt sie aus allen Nav-Flächen (Sidebar/CommandPalette/MobileTabbar); Routen + Code bleiben für V2. **Mein Team** zusätzlich für `admin` + `support` sichtbar. **PROJ-3:** im eingebetteten Einheiten-Detail (Master-Detail) sind die projekt-scoped Tabs Bankdaten/Karte/Finanzierer-Pool ausgeblendet (`!embedded`-Gate) — die Projekt-Pill-Tabs Lage/Bankdaten/Pool liefern dasselbe. tsc + build grün. **Bug-Hinweis:** gemeldeter 500 auf Kunde `c3e9e8fa…` — alle DB-Queries dieses Kunden sind in den Supabase-Logs `200` (Datenebene gesund), Ursache vermutlich transient. Logs zeigen aber reale wiederkehrende `statement timeout`-Fehler (separat zu adressieren).

> **PROJ-21 Einheiten-Vollständigkeit + Freigabe-Gate GEBAUT (2026-06-16):** Kundenwunsch (Chris/Leon, 15.6.) umgesetzt — voller Felddatensatz je Einheit + hartes Qualitätstor. Neue DB-Spalten (Migration `20260616000000`, auf Prod, 0 neue ERROR-Advisors): Enum `einheit_freigabe_status` (entwurf/in_bearbeitung/freigegeben, orthogonal zum Verkaufs-Status) + `freigegeben_at`, `kaufpreis_wohnung/_moebel`, `instandhaltungsruecklage_gesamt`, `renovierungen` (jsonb `[{gewerk,jahr}]`), `lage_im_haus`, `tags` (text[]), `standort_highlights`. `kaufpreis` bleibt der maßgebliche GESAMT-Wert (Engine unverändert). **SSOT** `src/lib/einheit-vollstaendigkeit.ts` (Pflichtfelder + bedingte Regeln vermietet_seit/renovierungen; KI-Felder via `kiPflichtAktiv` erst mit PROJ-22 scharf) wird von Gate, „Fehlende Daten"-Ansicht und Bulk-Warnung gemeinsam genutzt. Server-Action **`setFreigabeStatus`** = harter Gate (re-validiert frische DB-Werte, `assertOrgAccess`, lehnt unvollständige Freigabe ab). UI: EinheitForm (Lage im Haus, Kaufpreis-Aufteilung, Instandhaltungsrücklage gesamt, neuer Renovierungen-Tab, Tags/Standort-Highlights), CompletenessCard→Freigabe-Gate (Client, Badge + fehlende-Daten-Liste + Buttons), Entwurf-Badges in Liste/Detail, Detail-Anzeige (Renovierungstabelle/Tags/Highlights), Bulk-Import (neue Spalten + weiche, nicht blockende Freigabe-Warnung + „als Entwurf importiert"-Hinweis). **„Freigegeben" sperrt bewusst KEINE Aktionen** (nur online/vollständig-Flag) — kann später zum echten Tor ausgebaut werden. tsc + **184 Tests** + Build + Lint grün. **Offen:** Deploy auf Vercel, manuelle/E2E-Verifikation (eingeloggter VP), optionaler Listen-Filter nach freigabe_status, dann **PROJ-22** (KI füllt tags/standort_highlights → `kiPflichtAktiv` scharfschalten).

> **„In Entwicklung" zu Ende gebaut + Marketing-Site (2026-06-18):** Die vier auf der öffentlichen Roadmap als „In Entwicklung" geführten Features fertiggestellt. **PROJ-9 Provisionen:** war bereits vollständig (BUG-P1-Fix verifiziert) → nur Status auf Deployed. **PROJ-6 Finanzierungen:** echte Lücke geschlossen — es gab keinen App-Weg, einen Case zu erzeugen (DB-RPC `request_finanzierung` existierte, ungenutzt). Neu: Server-Action `requestFinanzierung` (authed Client, graceful „kein Pool"-Fehler) + Wrapper `listKundeCases` + „Finanzierung"-Reiter in `KundeDetailView` (zeigt Cases, Button „Finanzierung anfragen" mit Einheiten-Auswahl aus den Zuweisungen). **PROJ-22 KI:** Code war fertig; KI-Button in `EinheitForm` jetzt via `getAiStatus()` gegated (nur sichtbar wenn Server-Key gesetzt) → kein Ins-Leere-Klicken. **Live-Blocker bleibt:** `OPEN_API_KEY` muss in Vercel gesetzt werden (Nutzer-Aktion). **PROJ-23 Branding:** Logo wird jetzt serverseitig re-gehostet (neue Action `rehostLogoFromUrl`, SSRF-Guard wiederverwendet, Bucket `objekt-bilder/org-logos/{orgId}`, graceful Hotlink-Fallback) statt nur verlinkt; `createOrganisation` nimmt jetzt `logoUrl` an + Create-Org-Dialog übernimmt Logo. tsc + Build + Tests + Lint grün. **Marketing-Site (PROJ-2):** öffentliche Landingpage unter `/` (+ `/start`) und Produkt-Roadmap unter `/roadmap` live auf emi-hub.de (hell/seriös/kantig, Navy/Gold); Marketing-Routen in `proxy.ts` `PUBLIC_PREFIXES` freigegeben. **Offen:** visuelle Prod-Verifikation aller vier als eingeloggter VP/Admin; `OPEN_API_KEY` setzen; Finanzierer-Pools je Projekt befüllen (sonst schlägt `requestFinanzierung` fachlich fehl).

> **Projektansicht-Feinschliff (2026-06-18, PROJ-3):** Nutzer-Feedback an `ProjektDetailView`: (1) die drei Badges („Ab X € mtl. finanzieren", „Provisionsfrei", „Bestand·vermietet") entfernt; (2) Verkaufsstatus-Bar sitzt jetzt mittig zwischen Einheiten-Auswahl und Tabs; (3) Tab „Kalkulationen" ist Default (vorher Kaufpreisliste); (4) **gemeinsamer Calc-State** — neue `CalcBody`-Komponente (gekeyed per Einheit) hält EIN `useEinheitKalkulation` und versorgt rechte Investitions-Karte UND die Tab-Diagramme; Einheiten-Wechsel im Dropdown rechnet Preis (oben rechts) + Investition + Charts frisch durch; (5) **Annahmen-Slider in die rechte „Ihre Investition"-Karte** verschoben (neue kompakte `InvestitionsSliders`: Szenario, Eigenkapital, Zins, Tilgung, Haltedauer); AfA/KfW/voller Annahmen-Block bleibt im Kalkulations-Tab ausklappbar (`AnnahmenPanel`, geteilter State); (6) „Vermögensschere" + „Cashflow-Entwicklung" volle Breite + größer (`DiagrammePanel big`). Andere Nutzer von `DiagrammePanel`/`AnnahmenPanel`/`KalkulationsTab` unangetastet (nur additive Props/Exports). tsc + Build + Lint grün.

> **Supabase-Komplettmigration auf neuen Account + PROJ-30 Org-Custom-Domains gestartet (2026-06-22):** Nutzer wollte Supabase auf einen **neuen Account** (andere E-Mail) umziehen. **Migration durchgeführt** (`ffagjzkkzlywejzjfgue` → `gfbokqnjwmkcpcundtfs`, beide PG 17.6, Pro): portable pg_dump/psql 17.6 (lokal heruntergeladen, kein Docker), Dump (schema public + data + auth.users/identities + storage.buckets) → Restore mit `session_replication_role=replica` (Trigger/FK aus). **0 Fehler, alle Zeilenzahlen exakt zur Baseline** (einheiten 2132, bilder 7090, dokumente 9772, auth.users 21…), Schema identisch (30 Tab./45 Fn/129 Policies/40 Trigger/32 Enums), **RLS 30/30**. Lücke geschlossen: `on_auth_user_created`-Trigger (auth-Schema, nicht im public-Dump) nachgezogen. Storage **15/15** Dateien per Skript kopiert. `reservierungen-cron` Edge-Function via MCP deployed + pg_cron-Job neu (neue URL). Auth-Config (site_url + Redirect-Allowlist) per Management-API übertragen. **Vercel-Env-Swap** (URL/anon/service_role/ref/access_token) + **Redeploy** per Vercel-CLI → live auf www.emi-hub.de (Smoke 200/307). `.mcp.json` zweiter Server `supabase-new`. Altes Projekt bleibt als Backup. **PROJ-30 angelegt:** App soll kanonisch auf neutraler Vercel-URL laufen, Orgs verbinden eigene Domains (Vercel-API, Host→Org-Auflösung im Root-Layout); **emi-hub.de → Haupt-Org „Erfolg mit Immobilien"**. DB-Fundament (`organisationen.domain`+`domain_verified`, Migration `20260622120000`) live. **Offen:** Vercel-API-Client, Host-Auflösung, Actions, UI; Voraussetzung `VERCEL_API_TOKEN`.

## Next Available ID: PROJ-31
