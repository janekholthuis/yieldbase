import { ModuleStub } from "@/components/shell/ModuleStub";

export default function TicketsPage() {
  return (
    <ModuleStub
      title="Tickets"
      description="Support-Anfragen — wird im Migrations-Schritt PROJ-9 portiert."
    />
  );
}
